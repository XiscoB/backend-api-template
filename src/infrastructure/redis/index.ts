/**
 * Redis Infrastructure
 *
 * Optional Redis support for infrastructure features.
 * Redis is never required to boot - only used when explicitly configured.
 */
export { RedisModule } from './redis.module';
export { RedisService } from './redis.service';
