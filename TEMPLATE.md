# Backend API Template

This repository is a **template**, not a deployable product.

Before using it:

1. Clone the repository
2. Rename the project
3. Update README.md (project section only)
4. Configure environment variables
5. Do NOT modify authentication or role logic

If you need to change authentication behavior, do it in the auth platform — not here.
